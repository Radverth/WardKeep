# Godot editor/import cache — regenerated by `godot --import`.
.godot/
# Export output.
build/
# Local Godot editor overrides.
*.translation
.import/
# Keystores must never be committed (CI_SETUP.md).
*.keystore
*.jks
